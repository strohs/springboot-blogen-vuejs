import Navbar from '@/components/Navbar'

describe('Navbar', () => {
  // Evaluate the results of functions im
  // the raw component options
  it('has a computed object', () => {
    expect(Navbar.computed).to.be.a('object')
  })
})
